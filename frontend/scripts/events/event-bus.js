//export const EventBus = new EventTarget();

// future note: i dont necessarily have to use an event bus as an issue is figuring out how to remove events/checking
// if an event is already being listened for. i can just use the DOM elements instead