/*
export const onUpdateIngredients = 'on-update-ingredients';

export function createUpdateIngredientsEvent(ingredients) {
  return new CustomEvent(onUpdateIngredients, {
    detail: {
      ingredients
    }
  });
}

export const onIngredientsUpdated = 'on-ingredients-updated';

export function createIngredientsUpdatedEvent() {
  return new CustomEvent(onIngredientsUpdated);
}

export function updateIngredients2(ingredientObject, quantity) {
  return new CustomEvent(onUpdateIngredients, {
    detail: {
      ingredient: ingredientObject,
      quantity: quantity
    }
  });
}
*/